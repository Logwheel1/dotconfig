#! /bin/sh

chosen=$(printf "  Power Off\n  Restart\n" | rofi -dmenu -i -theme-str '@import "power.rasi"')

case "$chosen" in
	"  Power Off") poweroff ;;
	"  Restart") reboot ;;
	*) exit 1 ;;
esac
